#include<bits/stdc++.h>
#define endl '\n'
using namespace std;
const int MAXN = 2*10e3 + 2;
int nums1[MAXN],nums2[MAXN];
int mai(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n,m;
    cin>>n>>m;
    for(int i=0;i<n;++i){
        cin>>nums1[i];
    }
    for(int i=0;i<m;++i){
        cin>>nums2[i];
    }
    int max=-1,l1,r1,l2,r2;
    for(int i=0;i<)
return 0;
}