#include <bits/stdc++.h>
#define endl '\n'
using namespace std;
int N, K;
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    cin >> N;
    int a, b;
    cin >> a >> a >> a >> a >> a >> a;
    cin >> K;
    cin >> b >> b >> b >> b;
    cout << 1 << " " << 14 << endl;
    return 0;
}
