#include <bits/stdc++.h>
#define endl '\n'
using namespace std;
int a[100002], b[1003];
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int N, K;
    for (int i=0; i<N; i++) cin >> a[i];
    for (int i=0; i<K; i++) cin >> b[i];
    cout << 0 << endl;
    return 0;
}
